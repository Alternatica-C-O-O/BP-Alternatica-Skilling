server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Alternatica-C-O-O/skilling-config-repo.git
          search-paths:
            - '{application}'
          default-label: master
          username: JaremGallegos
          password: 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

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    prefer-ip-address: true